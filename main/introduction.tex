\chapter{Introduction}
\label{chp:introduction} 

\newacronym{mpc}{MPC}{Multi-Party Computation}
\newacronym{gc}{GC}{Garbled Circuit}
\newacronym{2pc}{2PC}{Two-party Computation}
\newacronym{ssp}{SSP}{state-separating proof}
\newacronym{gs}{GS}{garbling scheme}
\newacronym{ind-cpa}{IND-CPA}{Indistinguishability under Chosen Plaintext Attacks}
\newacronym{prf}{PRF}{Pseudo-Random Function}
\newacronym{prg}{PRG}{Pseudo-Random Generator}
\newacronym{smt}{SMT}{Satisfiability Modulo Theory}
\newacronym{cr}{CR}{correlation-robust}
\newacronym{ccr}{CCR}{circular correlation-robust}
\newacronym{otp}{OTP}{one-time pad}
\textcolor{red}{One more pass needed after writing conclusion}

In Secure Multiparty Computation (\acrshort{mpc}), two or more parties want to compute a common function using their individual inputs, but do not want to share these inputs with each other.
Common use cases of \acrshort{mpc} today include delegating computation to cloud servers, or querying third party datasets that cannot be shared directly.
 
Since remote servers are often owned and operated by a separate entity, it is difficult to obtain the same security and privacy guarantees that a user's own machine can provide. Besides user requirements, cybersecurity regulations also require methods to secure the data being processed from the data processor, such as in the case of private healthcare data, or other forms of personal data \cite{gdpr_2016}. 

\acrfull{2pc} is a special case of \acrshort{mpc} in which only two parties are involved in the protocol. 
Often attributed to Andrew Yao's seminal work in 1986 \cite{FOCS:Yao86}, the first approach for \acrshort{2pc} was to represent the function to be computed as a boolean circuit, and to use a \acrfull{gc} to compute the output of the function, while maintaining the secrecy of the inputs of both parties. For more than a decade after this work, \acrlong{gc}s were considered to not be practical because of the high bandwidth and computation costs of garbling, and due to the fact that garbling an arbitrary function into an optimized boolean circuit is difficult. However, there has been work in the field that has led to optimizations that allow them to be somewhat usable in generic \acrshort{2pc} systems like Fairplay \cite{malkhi_fairplaysecure_2004}, and in niche applications such as smart contracts \cite{haloani_coti_nodate}. 

Yao's version of \acrlong{gc}s used four ciphertext computations and transmissions per gate in the circuit. It relied on the assumption of existence of secure \acrlong{prf}s (\acrshort{prf}s) that could be used to build a secure encryption scheme. While this is a widely used assumption when designing cryptosystems from scratch, it is very idealistic. \citeauthor*{STOC:BeaMicRog90} published the next well-known optimization to garbled circuits, often called \acrfull{pnp} (we discuss it in more detail in Section \ref{subsec:pointandpermute}), which reduces the computational cost of evaluation of the circuit by up to $75\%$ of Yao's construction, and uses a \acrfull{prg} security assumption. We observe that while there are improvements, the core assumptions vary. This is a pattern that continues with \citeauthor*{ACMCEC:NaPiSu99}'s \acrlong{grr}-3 scheme (Section \ref{subsec:grr3}), which offers a flat $25\%$ reduction in bandwidth cost, but returns to a \acrshort{prf} security assumption. The next improvement - the Free XOR scheme by \citeauthor{ALP:KolSch08} can garble an XOR gate completely free of cost (Section \ref{subsec:freexor}), which can be very efficient if circuits are optimized to maximize the number of XOR gates. However, Free XOR relies on a entirely new assumption, `\acrlong{cr}ness' (\acrshort{cr}) of a hash function. Furthermore, in their paper, Free XOR was only proven in the stronger Random Oracle model, and \acrshort{cr} was later found to be inadequate. Free XOR was later proven secure using other models such as \acrfull{linrkkdm} security of encryption schemes and \acrlong{ccr}ness (\acrshort{ccr}) of hash functions. Both of these assumptions are weaker than Random Oracle, but stronger than \acrshort{cr}. The Half-Gates \cite{EC:ZahRosEva15} \acrlong{gs} was designed under the \acrshort{ccr} assumption, and leads to a $50\%$ decrease in bandwidth use over Yao's scheme.

Unlike sofware verification, proof verification is not concerned with whether an implementation faithfully realises a protocol, but whether the mathematical argument establishing the security of the proof is logically sound. These concerns are orthogonal: an implementation may be correct while relying on an incorrect security proof, just as a correct proof does not guarantee a secure implementation. Validating a large cryptographic proof however is a difficult task, mainly because reasoning about code by simply looking at it relies on intuition, and my hide away corner cases especially in larger proofs. \citeauthor{EPRINT:Halevi05b} argued that an alternative is to use computers to enhance the process of reasoning about code \cite{EPRINT:Halevi05b}. Two main approaches exist for automated verification of code - symbolic and computational. Symbolic verification uses an idealized model of cryptography (specifically, the Dolev-Yao model \cite{dolev-yao}) where primitives are treated as perfect black boxes. An adversary is only allowed to use these black boxes in its operations, limiting the list of operations it can perform to just such functions and their defined properties. This allows symbolic verification to be faster and makes it easier to build formal verification tools. For example, in the symbolic model, an encryption function may be modelled such that the adversary can derive the message only if they know the key. In contrast, the computational model expicitly models these cryptographic primitives as polynomial time reductions to some underlying assumption or game. The amount of `assumed code' is much smaller and the only limit to the adversary is that it must be a polynomial time algorithm. Computational verifiers reason about the security of cryptographic algorithms under given computational assumptions, as opposed to symbolic verifiers which assume that cryptographic primitives always satisfy their symbolic equations. However, this leads to proofs that are written for computational verifiers to be more complex. Examples of symbolic verification tools include ProVerif \cite{proverif} and Tamarin \cite{tamarin}, while EasyCrypt \cite{easycrypt}, CryptoVerif \cite{cryptoverif} and Domino \cite{domino:code} are examples of computational verification tools.  

The security of cryptographic algorithms is often reasoned about as `games' in an ideal and real world. The games consist of `oracles' which are basically procedures that expose certain functionality and either return an output or modify some state. An adversary interacts with the game in both worlds by calling its oracles and attempts to distinguish between the behavior of the two games. These games were initally expressed as turing machines with polynomial running time, but \citeauthor*{EC:BelRog06} reasoned that pseudocode was also a valid medium to express such games, and was suitable to use in future automated verification systems that \citeauthor{EPRINT:Halevi05b} wrote about \cite{EC:BelRog06}. Going a step further from \citeauthor{EC:BelRog06}'s code-based game-playing, \citeauthor{brzuska_state_2018} proposed state-separation, which would introduce the concept of games composed of packages with internal states which were modifiable by the oracles contained in those packages \cite{brzuska_state_2018}. State-separating proofs (\acrshort{ssp}s) are attractive because they allow cryptgraphers to divide their games into reusable components that have their own state, so a simple state modification does not invalidate game-wide assumptions made during a previous state. Induction based reasoning also becomes easier since the amount of code affected by a change of state is smaller since code is divided into packages. Furthermore, having reusable packages allow underlying constructs to be reused in other proofs that rely on similar primitives. It also allows for nice visualisations of package compositions as graphs.

It is worth noting that a proof of \citeauthor{FOCS:Yao86}'s \acrlong{gc} construction has already been expressed as a \acrshort{ssp} by \citeauthor{CSF:BrzOec23} \cite{CSF:BrzOec23}. This is a proof of the \acrlong{gs}, rather than the \acrlong{gc} protocol. This thesis intends to build on top of this work by converting \citeauthor{ALP:KolSch08}'s proof for the Free XOR \acrlong{gs} under the \acrshort{linrkkdm} assumption into a \acrshort{ssp}. The choice is primarily driven by the fact that significant future work in \acrlong{gs}s such as Half-Gates \cite{EC:ZahRosEva15} and Three-Halves \cite{threehalves} use it as a crucial component. The improvements in these schemes are not achievable without using Free XOR.  

Domino is a highly automated proof assistant for \acrshort{ssp}s that allows expression of games, packages and oracles in a syntax intentionally very similar to pen-and-paper \acrshort{ssp}s. Currently available as a standalone executable (`crate') in the Rust package manager (cargo), it includes a command-line interface that validates \acrshort{ssp} structures, provides type-checking and provides a decent degree of error handling. It also comes with a minimal extension for the Visual Studio Code text editor that provides syntax highlighting. Domino uses an underlying \acrfull{smt} solver to verify equivalence relations between games with different code, and also verifies reduction game-hops. It is worth noting that there is ongoing work in encoding and validating \citeauthor{CSF:BrzOec23}'s \acrlong{ssp} for Yao's \acrlong{gs} in Domino \cite{domino:paper}. This work shows the feasibility of encoding the Free XOR garbling scheme in Domino, and therefore, we have this as an additional goal for this thesis. We chose to encode \citeauthor{JC:Applebaum16}'s variation of Free XOR in this thesis because of its closeness to \citeauthor{FOCS:Yao86}'s scheme - both of them use encryption to generate the garblings, instead of a hash-based \acrfull{otp} used in \citeauthor{ALP:KolSch08}'s work. While the hash-based construction is more efficient, it uses a stronger Random Oracle assumption, which is harder to translate to real life, whereas \citeauthor{JC:Applebaum16}'s scheme comes with a description of how one can construct a \acrlong{linrkkdm} secure encryption scheme using an underlying \acrfull{lpn} assumption.

It is worth mentioning that this thesis initially aimed to formally verify the Half-Gates \acrlong{gs}, but ran into the research gap that exists between \acrlong{ssp}s for \citeauthor{FOCS:Yao86}'s \acrlong{gs}, and Half-Gates. Since Half-Gates builds on top of the Free XOR scheme, this thesis marks the first steps towards formally verifying the Half-Gates scheme, going as far as Free XOR.

\section{Objectives}
\label{sec:objectives}
The goals of this thesis can be summarized as follows:
\begin{enumerate}
    \item To develop a State Separating pen-and-paper reduction proof for the Free XOR garbling scheme.
    \item To encode this proof and verify its correctness in Domino.
\end{enumerate}

\section{Sustainability}
\label{sec:sustainability}
\newacronym{un}{UN}{United Nations}
\newacronym{sdg}{SDG}{Sustainable Development Goal}
While theoretical cryptography itself does not map well to any of the \acrfull{un} \acrfull{sdg}s, since the indicators for each subgoal are very distinct, the following arguments can be made for the contributions that this thesis may make to the targets of various subgoals:
\begin{itemize}
    \item \textbf{Target 9.5 - Enhance research and upgrade industrial technologies:} This work pushes the boundaries of research in verified proofs of cryptographic algorithms and protocols that are used to build software technologies.
    \item \textbf{Target 16.6 - Develop effective, accountable and transparent institutions:} This work is in the domain of privacy-preserving technologies, which when implemented into software can allow institutions to be more transparent while retaining the privacy of information. 
\end{itemize}   

The rest of this thesis is organized as follows: Chapter \ref{chp:background} contains background information on \acrlong{gs}s, optimizations, SSPs, and Domino. Chapter \ref{chp:penpaperssp} details the converted pen-and-paper proof for Free XOR. Chapter \ref{chp:domino} contains further details about the proof tool Domino, a brief overview on how to set it up, and an encoding of the \acrlong{ssp} in Domino. Chapter \ref{chp:discussion} discusses the advantages and challenges of working with Domino as opposed to plain pen-and-paper \acrshort{ssp}. Chapter \ref{chp:conclusion} contains concluding remarks and future work. 

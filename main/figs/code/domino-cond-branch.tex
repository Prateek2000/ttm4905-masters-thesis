\lstdefinelanguage{Domino}{
    % morecomment=[s]{/*}{*/},
    moredelim=[is][\color{gray}]{@@}{@@}
}
\begin{lstlisting}[
    language=Domino,
    caption={Conditional branching in \code{ENC}},
    label={lst:conditionalbranching},
    commentstyle=\small\rmfamily\itshape,
    basicstyle=\small\ttfamily,
    columns=fixed]
    package LINRKKDM {
    @@ ... @@
    oracle ENC(key: Bits(n), msg: Bits(n), z: Bool) -> Bits(m) {
    @@ ... @@
    if z {
      return enc(
        xor_(Unwrap(delta),key),
        xor_(msg,Unwrap(delta)), r); 
    } else {
      return enc(xor_(Unwrap(delta), key), msg, r);
    }
    @@ ... @@
    }}
\end{lstlisting}

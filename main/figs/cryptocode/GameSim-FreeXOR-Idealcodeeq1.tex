\begin{figure}[ht]
\centering
    \procedureblock[]{$G^{1\prime}_\mathsf{Sim\text{-}FreeXOR}$}{
        \underline{\text{State:}} \\
        \Delta :\text{global circuit offset}\\
%
        \underline{\underline{\mathsf{GARBLE\mathit{(f(x), \Phi(f))}}}} \\
        c \gets f(x) \pccomment{This is 0 since we set a and b to 0}\\
        \Delta \sample \bin^\secpar \ \pccomment{this is equivalent to the}\\
        \t\t\t\t\t\t\pccomment{if statement in the ideal version of $\rdv \to G^{1}_{\mathsf{LIN-RK-KDM}}$}\\
        (a, b) \gets (0,0) \\
        A_a, B_a, C_a \sample \bin^\secpar \\
        R_{00}, R_{01}, R_{10}, R_{11} \sample \bin^\secpar \\
        F: \text{Table} \\
        F[a][b] \sample (\enc(A_a, R_{00}),\enc(B_b, R_{00} \xor C_c)) \\
        F[a][b\xor 1] \sample (\enc(A_a, R_{01}),\enc(B_b \xor \Delta, 0^\secpar)) \\
        {\color{red}\pcbox{\color{black}F[a\xor 1][b] \sample (\enc(A_a \xor \Delta, 0^\secpar),\enc(B_b, R_{10} \xor C_c \xor \Delta))}} \\
        F[a\xor 1][b\xor 1] \sample (\enc(A_a \xor \Delta, 0^\secpar),\enc(B_b \xor \Delta, 0^\secpar)) \\
        X \gets (A_a, B_b) \\
        d \gets  \{C_c: c\} \\
        \pcreturn (F,X,d) \\
       % 
    }

\caption{Code for $G^{1\prime}_\mathsf{Sim\text{-}FreeXOR}$}
\label{fig:simfreexor-ideal-game-code-eq-1}
\end{figure}

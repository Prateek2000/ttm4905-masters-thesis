\chapter{Pen-and-Paper State-Separating Proof}
\label{chp:penpaperssp}
This chapter introduces the notion of a State Separating Proof, and how it differs from a traditional Game-playing proof. \textcolor{red}{is game-playing the correct term here?} Then, we define our pen-and-paper \acrlong{ssp} for the Free XOR \acrlong{gs} here.
\section{State Separating Proofs}
\label{sec:ssp}
\begin{itemize}
    \item What are oracles, games and packages. Refer \cite{brzuska_state_2018} for syntax.
    \item Something about why state separating proofs exist. "composable" and "constructive" security
    \item State separating proofs allow us to make intermediate arguments (hybrids) in a reusable manner. It is often more intuitive to model something as its own package that has inputs and outputs than to have it be part of many different codes and realise that its the same thing.
\end{itemize}
Talk a bit about the simulator based notation in \cite{CSF:BrzOec23}.
Say that we work on freexor, but only for a single AND gate. we believe this is sufficient since {AND, NOT, XOR} is functionally complete (just {AND, NOT} is too, but XOR is the optimization here).


\section{State Separating Proof of security of the garbling of an AND gate}
\label{sec:sspANDgate}
Talk about the FreeXOR variation in \cite{ALP:KolSch08}, and the different underlying assumption in \cite{JC:Applebaum16}. Say why we picked this notion (is there even any good reason? maybe because the paper came with a underlying construction).
\begin{itemize}
    \item define security again (prvsim)
    \item define the simualtor
    \item make the game hopping table thing to show code equivalence
    \item tadaaa
\end{itemize}
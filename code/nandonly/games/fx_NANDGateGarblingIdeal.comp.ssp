composition fx_NANDGateGarblingIdeal {
    const k: Integer;
    const Enc: fn (Bits(k), Bool), (Bits(k), Bool),(Bits(k), Bool) -> (Bits(k), Bool);
    instance garbleNANDgate = Sim_fx_NANDgate {
        params{
            k: k,
            Enc: Enc
        }
    }

    instance XOR = XOR {

    }

    compose {
        adversary: {
            GBLNANDGate: garbleNANDgate,
        }
        garbleNANDgate: {
            XOR: XOR,
        }
    }
}